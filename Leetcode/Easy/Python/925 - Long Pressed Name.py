class Solution:
    def isLongPressedName(self, name: str, typed: str) -> bool:
        for x in range(len(name)):
            for y in range(len(typed)):
                # check if name[x] == typed[y] and if that letter is repeated
                # if repeated less in x than y continue while keeping track of the indicies
                print("need to finish")

        return("true or false")
